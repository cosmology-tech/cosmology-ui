import { SwapModal } from '@cosmology-ui/react';
import { ArgsTable, Canvas, Meta, Source, Story } from '@storybook/addon-docs';

import { SwapModalStory } from '../../ui-kits/modals/swap/SwapModal.stories.tsx';

# Swap Modal

<Meta
  component={SwapModal}
  title="components/Swap"
  parameters={{
    docs: {
      source: {
        language: 'tsx',
        format: true,
        code: `import { SwapModal } from '@cosmology-ui/react';`
      }
    },
    controls: {
      include: []
    }
  }}
/>

<Canvas withSource="close">
  <Story story={SwapModalStory} />
</Canvas>

<ArgsTable of={SwapModal} />

<Source
  language="tsx"
  dark
  format={true}
  code={`
      <SwapModal
        isOpen={isOpen}
        loading={loading}
        fromToken={fromToken}
        toToken={toToken}
        dropdownData={dropdownData}
        inputData={inputData}
        tokenPrice={tokenPrice}
        swapDetails={swapDetails}
        slippageConfig={slippageConfig}
        submitButtonConfig={submitButtonConfig}
        onFromDropdownChange={onFromDropdownChange}
        onToDropdownChange={onToDropdownChange}
        onAmountInputChange={onAmountInputChange}
        onSwapSwitch={onSwapSwitch}
        onSwapSubmit={onSwapSubmit}
        onClose={onClose}
      />
  `}
/>
